name: PR Validation

on:
  pull_request:
    branches: [main]

permissions:
  contents: read
  pull-requests: write

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: |
          make install
          npm install -g aws-cdk

      - name: Run linting
        id: lint
        run: make lint
        continue-on-error: true

      - name: Run unit tests
        id: test-unit
        run: make test-unit

      - name: Run CDK tests
        id: test-cdk
        run: make test-cdk

      - name: Synthesize CDK stack
        id: synth
        run: make synth

      - name: Comment PR with results
        if: always()
        uses: actions/github-script@v7
        with:
          script: |
            const output = `#### Test Results
            - Linting: ${{ steps.lint.outcome == 'success' && '✅' || '⚠️' }}
            - Unit Tests: ${{ steps.test-unit.outcome == 'success' && '✅' || '❌' }}
            - CDK Tests: ${{ steps.test-cdk.outcome == 'success' && '✅' || '❌' }}
            - CDK Synth: ${{ steps.synth.outcome == 'success' && '✅' || '❌' }}

            **Note:** Integration tests run after merge to main.`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            });
